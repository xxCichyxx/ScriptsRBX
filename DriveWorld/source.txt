local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Window = Rayfield:CreateWindow({
    Name = "Drive World",
    LoadingTitle = "Loading...",
    LoadingSubtitle = "by xxCichyxx",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = nil,
        FileName = "driveworld_config"
    },
    KeySystem = false,
})
local AutoFarm = loadstring(game:HttpGet('https://raw.githubusercontent.com/xxCichyxx/ScriptsRBX/refs/heads/main/DriveWorld/DriveWorldairfarm.txt'))()
local AutoFarm1 = loadstring(game:HttpGet('https://raw.githubusercontent.com/xxCichyxx/ScriptsRBX/refs/heads/main/DriveWorld/DriveWorldairfarm1.txt'))()
local AutoFarm2 = loadstring(game:HttpGet('https://raw.githubusercontent.com/xxCichyxx/ScriptsRBX/refs/heads/main/DriveWorld/DriveWorlddriftfram1.txt'))()
local TabAutoFarms = Window:CreateTab("AutoFarms", "tree-palm")
local AutoFarm_Toggle = TabAutoFarms:CreateToggle({
    Name = "AutoFarm Air 0.1",
    CurrentValue = false,
    Flag = "ToggleAutoFarm",
    Callback = function(Value)
        if Value then
            AutoFarm.startMonitoring()  -- Rozpoczynamy AutoFarm
        else
            AutoFarm.stopMonitoring()  -- Zatrzymujemy AutoFarm
        end
    end,
})

local AutoFarm_Toggle2 = TabAutoFarms:CreateToggle({
    Name = "AutoFarm Air 0.2",
    CurrentValue = false,
    Flag = "ToggleAutoFarm",
    Callback = function(Value)
        if Value then
        AutoFarm1.startMonitoring()  -- Rozpoczynamy AutoFarm
        else
        AutoFarm1.stopMonitoring()  -- Zatrzymujemy AutoFarm
        end
    end,
})

local AutoFarm_Toggle3 = TabAutoFarms:CreateToggle({
    Name = "AutoFarm Drift 0.1",
    CurrentValue = false,
    Flag = "ToggleAutoFarm",
    Callback = function(Value)
        if Value then
        AutoFarm2.startMonitoring()  -- Rozpoczynamy AutoFarm
        else
        AutoFarm2.stopMonitoring()  -- Zatrzymujemy AutoFarm
        end
    end,
})
wait(0.5)
local bb = game:service'VirtualUser'
game:service'Players'.LocalPlayer.Idled:connect(function()
    bb:CaptureController()
    bb:ClickButton2(Vector2.new())  -- Symuluje kliknięcie prawym przyciskiem myszy
end)
